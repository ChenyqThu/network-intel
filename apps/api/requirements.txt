# Runtime dependencies for the nintel backend (pinned).
# Mirrors pyproject.toml [project.dependencies]; install with `pip install -r requirements.txt`.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
jinja2==3.1.5
jsonschema==4.23.0
python-dotenv==1.0.1

# Test + dev tooling
pytest==8.3.4
httpx==0.28.1

# Optional — only needed when NINTEL_LLM_ENABLED=true (live curation/classification).
# anthropic==0.42.0
